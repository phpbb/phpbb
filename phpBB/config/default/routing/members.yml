phpbb_members_online:
    path: /
    defaults:
        _controller: members.controller.online:handle

phpbb_members_online_whois:
    path: /whois/{session_id}
    defaults:
        _controller: members.controller.online.whois:handle

phpbb_members_team:
    path:             /team
    defaults:
        _controller:  members.controller.team:handle
