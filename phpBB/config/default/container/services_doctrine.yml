services:
    doctrine.connection:
        class: Doctrine\DBAL\Connection
        factory: ['phpbb\db\connection_factory', 'get_connection']
        arguments:
            - '@config.php'
            - '@doctrine.cache'

    doctrine.cache:
        class: phpbb\cache\doctrine_bridge
        arguments:
            - '@cache.driver'
