services:
    filesystem:
        class: phpbb\filesystem\filesystem

    files.filespec:
        class: phpbb\files\filespec
        arguments:
            - @filesystem
            - @mimetype.guesser
            - @plupload
