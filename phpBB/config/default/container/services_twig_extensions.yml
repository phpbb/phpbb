# Twig extensions don't needed by the installer

services:
    template.twig.extensions.jumpbox:
        class: phpbb\template\twig\extension\jumpbox
        arguments:
            - '@auth'
            - '@dbal.conn'
            - '@dispatcher'
            - '@path_helper'
        tags:
            - { name: twig.extension }
